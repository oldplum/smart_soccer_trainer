Third-party licenses and attributions

- Main license for this repository: Apache License 2.0 (file: `LICENSE`).

Scan summary (asset-focused):
- 我在仓库中扫描了 `common_components/*/assets` 以及常见许可头（`SPDX-License-Identifier`）。
- 结论：未在源码或资产中检测到 `CC-BY-SA`（Creative Commons Attribution-ShareAlike）许可头或标注。多数源码为 `Apache-2.0`，部分资源为 `CC0-1.0`（公共领域样式），少数生成文件使用 `LicenseRef-Espressif-Modified-MIT`。

Files with non-Apache notices should be reviewed individually. See `LICENSE` for the repository license.

Recommended next steps:
1. If you want full assurance, generate a per-file SPDX report (I can do this).
2. If you confirm no CC‑BY‑SA assets are required, it's safe to remove the `LICENSE-CC-BY-SA` placeholder (already removed).
3. Optionally add a `NOTICE` file aggregating third-party attributions required by Apache‑2.0.

If you want me to run a full per-file SPDX report now, say so and I will produce it.